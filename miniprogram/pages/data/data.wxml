<view class="container">
  <view class="tabs">
    <block wx:for="{{tabs}}" wx:key="key">
      <view class="tab {{active===item.key?'active':''}}" bindtap="switchTab" data-key="{{item.key}}">{{item.text}}</view>
    </block>
  </view>

  <view class="panel" wx:if="{{active==='weight'}}">体重记录、趋势、目标设定（示例占位）</view>
  <view class="panel" wx:if="{{active==='diet'}}">饮食记录、营养分析、热量统计（示例占位）</view>
  <view class="panel" wx:if="{{active==='exercise'}}">运动记录、消耗计算、计划推荐（示例占位）</view>
  <view class="panel" wx:if="{{active==='plan'}}">我的方案、对比、执行跟踪（示例占位）</view>
</view>